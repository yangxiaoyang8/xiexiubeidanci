{
  "version": 2,
  "buildCommand": "cd server && pnpm install && pnpm build",
  "outputDirectory": "server/dist",
  "builds": [
    {
      "src": "api/index.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/api/index.ts"
    }
  ]
}
